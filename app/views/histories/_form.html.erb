<%= form_with(model: history) do |form| %>
  <% if history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
        <% history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pet, "Mascota",  style: "display: block" %>
    <%= form.select :pet_id, @pets.map { |pet| [pet.name, pet.id] }, prompt: "Seleccione una mascota..." %>
  </div>

  <div class="mt-3">
    <%= form.label :controldate, "Fecha Control", style: "display: block" %>
    <%= form.date_field :controldate %>
  </div>

  <div class="mt-3">
    <%= form.label :wheight, "Peso(gr)",  style: "display: block" %>
    <%= form.text_field :wheight %>
  </div>

  <div class="mt-3">
    <%= form.label :height, "Altura(cm)",  style: "display: block" %>
    <%= form.text_field :height %>
  </div>

  <div class="mt-3">
    <%= form.label :description, "Descripción",  style: "display: block" %>
    <%= form.text_area :description %>
  </div>


  <div>
    <%= form.submit "Guardar" %>
  </div>
<% end %>
