<p style="color: green"><%= notice %></p>

<h1>Clientes</h1>

<%= link_to "Nuevo Cliente", new_client_path %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>id</th>
      <th>Nombre</th>
      <th>Email</th>
      <th>Teléfono</th>
      <th>Cant de Mascotas</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @clients.each do |client| %>
    <tr>
      <td><%= client.id %></td>
      <td><%= client.name %></td>
      <td><%= client.email %></td>
      <td><%= client.phone %></td>
      <td><%= client.pets_count %></td>
      <td><%= link_to "Ver", client %></td>
      <td><%= link_to "Editar", edit_client_path(client) %></td>
      <td><%= link_to "Borrar", client, data: { "turbo-method": :delete } %></td>
    </tr>
  <% end %>
  </tbody>
</table>
